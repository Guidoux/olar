<center>
  <div class="jumbotron">
    <h1><%= t('.welcome_to') %> Olar!</h1>
    <p><%= t('.value_proposition') %></p>
    <br />
    <br />
    <p><%= link_to t('.sign_in_with', :provider => 'Facebook'), user_facebook_omniauth_authorize_path, class: 'btn btn-primary btn-lg' %></p>
  </div>
  <% I18n.available_locales.each do |locale| %>
    <%= link_to_unless_current t('language', :locale => locale), locale: locale.to_s %>    
  <% end %>
</center>